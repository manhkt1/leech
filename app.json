{
	"name": "cloud-torrent",
	"website": "https://github.com/manhkt1/leech",
	"repository": "https://github.com/manhkt1/leech",
	"env": {
		"TITLE": {
			"description": "Optionally set the title of this instance (defaults to 'rapidleech')",
			"required": false
		},
		"AUTH": {
			"description": "Optional HTTP authentication in the form user:password",
			"required": false
		}
	}
}
